name: "Prow github actions"
on:
  issue_comment:
    types: [created]
permissions:
  issues: write
  pull-requests: write

jobs:
  execute:
    runs-on: ubuntu-latest
    steps:
      - run: hub clone ${{ github.repositoryUrl}} .
      - run: ls
      - uses: carolynvs/prow-github-actions@owners-file2
        with:
          prow-commands: '/approve 
            /lgtm'
          github-token: "${{ secrets.GITHUB_TOKEN }}"
